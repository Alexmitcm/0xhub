# Production Environment Configuration for Render
NODE_ENV=production
PORT=8080

# Database Configuration (will be set by Render)
DATABASE_URL=${DATABASE_URL}
LENS_DATABASE_URL=${LENS_DATABASE_URL}
DIRECT_URL=${DIRECT_URL}

# Supabase Configuration (set these in Render dashboard)
SUPABASE_URL=${SUPABASE_URL}
SUPABASE_KEY=${SUPABASE_KEY}

# Lens Network Configuration
NEXT_PUBLIC_LENS_NETWORK=testnet
LENS_API_URL=https://api-mumbai.lens.dev

# Security Configuration (set these in Render dashboard)
PRIVATE_KEY=${PRIVATE_KEY}
SHARED_SECRET=${SHARED_SECRET}
JWT_SECRET=${JWT_SECRET}

# Contract Addresses (set these in Render dashboard)
REFERRAL_CONTRACT_ADDRESS=${REFERRAL_CONTRACT_ADDRESS}
BALANCED_GAME_VAULT_ADDRESS=${BALANCED_GAME_VAULT_ADDRESS}
UNBALANCED_GAME_VAULT_ADDRESS=${UNBALANCED_GAME_VAULT_ADDRESS}
VIP_VAULT_ADDRESS=${VIP_VAULT_ADDRESS}
USDT_CONTRACT_ADDRESS=${USDT_CONTRACT_ADDRESS}

# Infura Configuration (set these in Render dashboard)
INFURA_URL=${INFURA_URL}
INFURA_WS_URL=${INFURA_WS_URL}

# Default Configuration
DEFAULT_LENS_HANDLE=nftgamer.lens
PGPASSWORD=${PGPASSWORD}

# Redis Configuration (will be set by Render)
REDIS_URL=${REDIS_URL}

# Render API Configuration
RENDER_API_KEY=${RENDER_API_KEY}
